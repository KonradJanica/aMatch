<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference-->
        <!--android:key="example_text"-->
        <!--android:title="@string/pref_title_display_name"-->
        <!--android:defaultValue="@string/pref_default_display_name"-->
        <!--android:selectAllOnFocus="true"-->
        <!--android:inputType="textCapWords"-->
        <!--android:capitalize="words"-->
        <!--android:singleLine="true"-->
        <!--android:maxLines="1" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="topic_list"
        android:title="@string/pref_title_topic"
        android:defaultValue="None"
        android:entries="@array/pref_topic_list_titles"
        android:entryValues="@array/pref_topic_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="company_list"
        android:title="@string/pref_title_company"
        android:defaultValue="None"
        android:entries="@array/pref_company_list_titles"
        android:entryValues="@array/pref_company_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="position_list"
        android:title="@string/pref_title_position"
        android:defaultValue="None"
        android:entries="@array/pref_position_list_titles"
        android:entryValues="@array/pref_position_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <EditTextPreference
        android:key="page_number"
        android:title="@string/pref_page_number_title"
        android:defaultValue="1"
        android:inputType="numberSigned"
        />

</PreferenceScreen>
